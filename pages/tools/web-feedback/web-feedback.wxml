<!--pages/tools/web-feedback/web-feedback.wxml-->
<view class="layer content-wrap">
  <view wx:for="{{list}}" wx:key="index" wx:for-item="item" class="list-item">
    <view class="label">{{ item.label }}</view>
    <view class="value">{{ item.value }}</view>
  </view>
</view>
